{
  "name": "brightbound-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A container-ready HTTP API for BrightBound",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  }
}
